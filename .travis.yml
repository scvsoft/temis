language: node_js

node_js:
  - "stable"

#services:
#  - mongodb

cache:
  directories:
    - "server/node_modules"

install:
  - cd server
  - npm install

#before_script:
#  - sleep 15
#  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'

script:
  - npm test

