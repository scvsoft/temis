{
  "name": "temis-server",
  "version": "1.0.0",
  "description": "Backend for Temis: A voice to report acts of violence and create awareness",
  "main": "main.js",
  "scripts": {
    "precommit": "lint-staged",
    "prepush": "lint-staged && npm test",
    "prestart": "npm run lint",
    "start": "node src/main.js",
    "dev": "nodemon --exec npm start",
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "test-ci": "npm run lint && npm run test --coverage && cat ./coverage/lcov.info | coveralls",
    "build": "babel src -d dist",
    "serve": "node dist/main.js",
    "lint": "npm run lint:js -- --write **/*.{js,jsx} && npm run lint:json -- **/*.{json}",
    "lint:js": "prettier --config ../.prettierrc",
    "lint:json": "jsonlint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/scvsoft/temis"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "express-jwt": "^5.3.0",
    "jsonwebtoken": "^8.1.0",
    "passport": "^0.4.0",
    "passport-facebook-token": "^3.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "coveralls": "^3.0.0",
    "eslint": "^4.10.0",
    "eslint-config-react-app": "^2.0.1",
    "husky": "^0.14.3",
    "jest": "^21.2.1",
    "jsonlint": "^1.6.2",
    "lint-staged": "^4.3.0",
    "nock": "^9.0.28",
    "nodemon": "^1.12.1",
    "prettier": "^1.8.2"
  },
  "lint-staged": {
    "gitDir": "../",
    "linters": {
      "*.{js,jsx}": [
        "npm run lint:js -- --write",
        "git add"
      ],
      "*.json": [
        "npm run lint:json",
        "git add"
      ]
    }
  }
}
